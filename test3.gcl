|[
  declare
    max_ : int;
    A : array[0..1]
    A := 1,2;
    // Asignar un valor a una variable de control para un for.
    max_ := A[0];
    for _ in 1 to 5  -->
       if max_ < A[_] -->
          max_ := A[_]
       [] max_ >= A[_] -->
          _ := 6
       fi
    rof
    // Esto es un comentario. Debe ser ignorado.
]| 